use crate::{
    Byte,
    TableAddr,
    MemAddr,
    FuncIdx,
};

pub fn init_elem(_tableaddr: &TableAddr, _offset: &u32, _funcindices: &Vec<FuncIdx>) {
    unimplemented!()
}

pub fn init_data(_memaddr: &MemAddr, _offset: &u32, _bytes: &Vec<Byte>) {
    unimplemented!()
}