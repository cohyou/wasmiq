use crate::{
    Store,
    Module,
    ExternVal,
    ModuleInst,
    Error,  
};

pub fn module_instanciate(store: Store, module: Module, externvals: Vec<ExternVal>) -> (Store, Result<ModuleInst, Error>) {
    unimplemented!();
}